<Window x:Class="WeatherApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        Title="Погода" Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource BgBrush}">
    
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Поиск + Тема -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <TextBox Width="300" Margin="0,0,10,0" 
                     Text="{Binding CityName, UpdateSourceTrigger=PropertyChanged}"
                     Background="{DynamicResource CardBg}" 
                     Foreground="{DynamicResource FgBrush}"/>
            <Button Content="🔍 Поиск" Width="100" Command="{Binding SearchWeatherCommand}"/>
            <ToggleButton Content="🌙" Width="40" Margin="10,0,0,0" 
                          IsChecked="{Binding IsDarkTheme}" ToolTip="Тема"/>
        </StackPanel>

        <!-- Ошибка -->
        <TextBlock Grid.Row="1" Text="{Binding ErrorMessage}" Foreground="Red" 
                   TextWrapping="Wrap" Margin="0,0,0,10"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource BoolToVis}, 
                              FallbackValue=Collapsed, TargetNullValue=Collapsed}"/>
        
        <!-- Загрузка -->
        <ProgressBar Grid.Row="1" Height="4" IsIndeterminate="True" 
                     Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}, 
                                FallbackValue=Collapsed}" 
                     Background="{DynamicResource CardBg}" Foreground="DodgerBlue" Margin="0,5,0,10"/>

        <!-- ✅ ТЕКУЩАЯ ПОГОДА (исправленная привязка Visibility) -->
        <Border Grid.Row="2" Background="{DynamicResource CardBg}" CornerRadius="10" Padding="15" Margin="0,0,0,15"
                Visibility="{Binding IsWeatherVisible, Converter={StaticResource BoolToVis}, 
                           FallbackValue=Collapsed}">
            <StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Image Source="{Binding CurrentWeather.IconUrl}" Width="80" Height="80"/>
                    <StackPanel Margin="10,0,0,0">
                        <TextBlock Text="{Binding CurrentWeather.City}" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource FgBrush}"/>
                        <TextBlock Text="{Binding CurrentWeather.Condition}" FontSize="16" Foreground="{DynamicResource FgBrush}" Opacity="0.8"/>
                        <TextBlock Text="{Binding CurrentWeather.Temperature, StringFormat='{}{0:F1}°C'}" FontSize="48" FontWeight="Light" Foreground="{DynamicResource FgBrush}"/>
                    </StackPanel>
                </StackPanel>
                <Separator Margin="0,15"/>
                <UniformGrid Columns="3" Margin="0,10,0,0">
                    <TextBlock TextAlignment="Center" Foreground="{DynamicResource FgBrush}">
                        <Run Text="💧 Влажность: "/><Run Text="{Binding CurrentWeather.Humidity, StringFormat='{}{0}%'}"/>
                    </TextBlock>
                    <TextBlock TextAlignment="Center" Foreground="{DynamicResource FgBrush}">
                        <Run Text="💨 Ветер: "/><Run Text="{Binding CurrentWeather.WindSpeed, StringFormat='{}{0:F1} м/с'}"/>
                    </TextBlock>
                    <TextBlock TextAlignment="Center" Foreground="{DynamicResource FgBrush}">
                        <Run Text="📊 Давление: "/><Run Text="{Binding CurrentWeather.Pressure, StringFormat='{}{0} мм рт. ст.'}"/>
                    </TextBlock>
                </UniformGrid>
            </StackPanel>
        </Border>

        <!-- Прогноз + История -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,0,10,0">
                <ListView ItemsSource="{Binding Forecast}" Background="{DynamicResource CardBg}" Foreground="{DynamicResource FgBrush}" BorderThickness="0">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="5">
                                <TextBlock Text="{Binding Date}" Width="70" FontWeight="Bold"/>
                                <TextBlock Text="{Binding Condition}" Width="120" TextTrimming="CharacterEllipsis"/>
                                <TextBlock Text="{Binding TempMin, StringFormat='{}{0:F0}°'}" Margin="10,0,5,0"/>
                                <TextBlock Text="{Binding TempMax, StringFormat='{}{0:F0}°'}" FontWeight="Bold"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </ScrollViewer>

            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                <TextBlock Text="📜 История" FontWeight="Bold" Margin="0,0,0,5" Foreground="{DynamicResource FgBrush}"/>
                <ListBox ItemsSource="{Binding History}" Background="{DynamicResource CardBg}" Foreground="{DynamicResource FgBrush}" BorderThickness="0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" Cursor="Hand" MouseLeftButtonUp="HistoryItem_Click"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </StackPanel>
        </Grid>
    </Grid>
</Window>